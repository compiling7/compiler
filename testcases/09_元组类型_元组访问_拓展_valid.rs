// ====================== 9 元组类型、元组访问(拓展) ======================

// 9.1 元组类型（元组类型不在语法支持范围内，以i32变量代替元组测试）
fn program_9_1() {
    let a:i32=0;
    let b:i32=1;
    let c:i32=2;
}

// 9.3 元组元素访问 .数字（不在语法支持范围内，以数组元素访问代替）
fn program_9_3__1(mut a:[i32;3]) {
}

fn program_9_3__2(mut a:[i32;3]) {
    let mut b:i32=a[0]+a[1]+a[2];
}

fn program_9_3__3(mut a:[i32;3]) {
    let mut b:i32=a[0];
    a[0]=1;
}

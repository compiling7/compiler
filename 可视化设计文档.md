# CompilerLab 可视化子系统设计文档（Web版）

## 一、概述

CompilerLab 是一个面向教育用途的类 Rust 语言编译器前端可视化系统。可视化子系统是整个项目中独立于编译器核心（词法分析、语法分析、语义分析、IR生成、汇编生成）的重要组成部分，其目标是将编译器各阶段产生的内部数据结构以直观、交互友好的方式呈现给用户，辅助编译原理课程的学习与理解。

可视化子系统采用 **Flask + D3.js v7 + Tailwind CSS** 技术栈，以 **前后端分离的 Web 单页应用（SPA）** 形态运行。后端提供 REST API 负责编译流水线的执行与数据序列化，前端负责数据的交互式呈现。

```
运行方式：
  开发模式： python web_app.py   → 浏览器访问 http://127.0.0.1:5000
  桌面壳：   python main_app.py   → PyWebView 原生窗口加载 Web 页面
```

---

## 二、整体架构

### 2.1 系统层次结构

```
┌─────────────────────────────────────────────────────────┐
│                    Web 前端 (SPA)                         │
│              templates/index.html                        │
│  ┌──────────┬──────────┬──────────┬──────────┐          │
│  │ Editor   │  Lexer   │  Parser  │   AST    │  ...     │
│  │ 视图     │  视图     │  视图     │  视图     │          │
│  └──────────┴──────────┴──────────┴──────────┘          │
│                  D3.js v7 · Tailwind CSS                 │
└─────────────────────────┬───────────────────────────────┘
                          │ HTTP JSON API
                          ▼
┌─────────────────────────────────────────────────────────┐
│               Flask 后端 (REST API)                       │
│                    web_app.py                            │
│  ┌──────────┬──────────┬──────────┬──────────┐          │
│  │ /api/lex │/api/parse│/api/sem  │/api/pipe │  ...     │
│  │          │          │antic     │line      │          │
│  └──────────┴──────────┴──────────┴──────────┘          │
│               _ast_to_dict 等序列化函数                   │
└─────────────────────────┬───────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                   编译器核心后端                           │
│  lexer → parser → semantic → ir_generator → asm_generator│
└─────────────────────────────────────────────────────────┘
```

### 2.2 功能总览

Web 版可视化子系统共提供 **15 项功能**，涵盖编译器前端流水线的各个阶段，分布在 7 个视图面板中：

| 视图 | 功能 | 技术实现 |
|------|------|----------|
| **Editor** | 源代码编辑器（带行号、Tab 支持、光标位置显示） | HTML `<textarea>` + JavaScript |
| **Editor** | 符号表展示（名称/类型/可变性） | HTML 表格渲染 |
| **Editor** | 编译统计概览（Token数/AST节点数/IR指令数/错误数） | JavaScript 统计计算 |
| **Lexer** | 语法着色代码展示 | CSS 语法着色类 (`.syn-kw`, `.syn-num` 等) |
| **Lexer** | Token 流表格（类型/值/行/列） | HTML 表格 + `renderLexer()` |
| **Lexer** | Token 悬停高亮联动（代码 ↔ Token 表） | JavaScript 事件绑定 |
| **Lexer** | Token 详情面板 + 统计信息 | 右侧详情面板动态更新 |
| **Parser** | D3.js 语法树图（基于追踪事件构建） | `d3.hierarchy()` + `d3.tree()` + `d3.linkVertical()` |
| **Parser** | 节点属性详情面板（悬停显示） | `showPSNode()` 事件处理 |
| **Parser** | 缩放/平移/适应（D3 zoom） | `d3.zoom()` 交互控制 |
| **LR** | 解析过程逐步动画（上一步/下一步/播放/暂停/调速） | `setInterval` + 游标控制 |
| **LR** | 递归调用栈可视化 + 文法规则实时显示 | 事件驱动更新 |
| **AST** | D3.js 抽象语法树（彩色编码节点） | `d3.hierarchy()` + `nColor()` 配色 |
| **AST** | 节点 Tooltip 信息展示 | 动态 `d3-tip` div 元素 |
| **Issues** | 错误/警告卡片列表 + 详情 | 动态 HTML 渲染 |

---

## 三、技术栈

| 层次 | 技术 | 用途 |
|------|------|------|
| 后端语言 | Python 3 | 编译器逻辑与 REST API |
| Web 框架 | Flask | HTTP 路由与 JSON 序列化 |
| 桌面壳 | PyWebView | 为 Web 前端提供原生桌面窗口 |
| 前端样式 | Tailwind CSS（CDN） | 响应式布局与 Material Design 3 风格 |
| 前端图标 | Google Material Symbols | 界面图标 |
| 前端字体 | Inter + JetBrains Mono（Google Fonts） | UI 字体与等宽代码字体 |
| 前端图表 | D3.js v7（CDN） | 树形可视化与交互式图形 |
| 打包工具 | PyInstaller | 构建独立可执行文件 |

---

## 四、详细设计

### 4.1 模块划分与文件结构

```
可视化子系统文件：
├── visualization.py          # 纯文本 AST 可视化（供后端 _compute_stats 使用）
├── web_app.py                # Flask 后端 + AST JSON 序列化（~460行）
├── main_app.py               # PyWebView 桌面壳入口（~87行）
├── build.py                  # PyInstaller 打包脚本
├── CompilerLab.spec          # PyInstaller 打包配置
└── templates/
    └── index.html            # Web 前端 SPA（~1240行，内联全部 CSS/JS）
```

关键依赖关系：

```
web_app.py
  ├── Lexer, Parser, SemanticAnalyzer, IRGenerator, AssemblyGenerator
  ├── compiler_ast (AST 节点定义)
  └── visualization (仅 _compute_stats 中引用 ASTVisualizer)

index.html
  └── 通过 HTTP API 调用 web_app.py 的所有端点
```

### 4.2 关键模块设计

#### 4.2.1 Flask REST API 后端 — `web_app.py`

**设计思路**：采用 RESTful 架构，每个编译器阶段对应一个独立的 API 端点，同时提供 `/api/pipeline` 端点一次性串联完整流水线。所有返回值统一为 JSON 格式。

**API 路由设计**：

| 路由 | 功能 | 输入 | 输出 |
|------|------|------|------|
| `POST /api/lex` | 词法分析 | `{source: string}` | `{tokens, errors}` |
| `POST /api/parse` | 语法分析 | `{source: string}` | `{ast, trace_events}` |
| `POST /api/semantic` | 语义分析 | `{source: string}` | `{ast, symbol_table, semantic_errors}` |
| `POST /api/ir` | IR生成 | `{source: string}` | `{quads}` |
| `POST /api/asm` | 汇编生成 | `{source: string}` | `{assembly, quads}` |
| `POST /api/pipeline` | 完整流水线 | `{source: string}` | `{tokens, ast, trace, quads, assembly, symbol_table, stats}` |

**核心序列化函数 `_ast_to_dict()`** — 前后端数据交换的桥梁：

```python
def _ast_to_dict(node):
    """Convert AST node to a dict suitable for JSON serialization & D3.js."""
    if node is None:
        return None

    d = {"name": node.node_name}  # 节点类型名（如 "FunctionDecl"）

    # 提取主标签
    if hasattr(node, "name") and not isinstance(getattr(node, "name", None), list):
        d["label"] = str(node.name)
    if hasattr(node, "value") and not isinstance(getattr(node, "value", None), (list, ASTNode)):
        d["label"] = str(node.value)
    if hasattr(node, "op"):
        d["label"] = str(node.op)
    if hasattr(node, "type_name"):
        d["label"] = str(node.type_name)

    # 提取附加信息（按节点类型定制）
    extras = []
    if isinstance(node, VarDeclStmtNode):
        extras.append(f"mut={node.is_mutable}")
    elif isinstance(node, NumberLiteralNode):
        extras.append(f"val={node.value}")
    elif isinstance(node, BlockStmtNode):
        extras.append(f"stmts={len(node.statements)}")
    elif isinstance(node, FuncCallNode):
        extras.append(f"args={len(node.args)}")
    if extras:
        d["extra"] = ", ".join(extras)

    # 递归处理子节点
    children = []
    if isinstance(node, ProgramNode):
        for c in node.declarations:
            children.append(_ast_to_dict(c))
    elif isinstance(node, FunctionDeclNode):
        if node.params:
            for p in node.params:
                children.append(_ast_to_dict(p))
        if node.return_type:
            children.append(_ast_to_dict(node.return_type))
        if node.body:
            children.append(_ast_to_dict(node.body))
    # ... 其余节点类型类似处理 ...

    if children:
        d["children"] = children
    return d
```

**关键设计点**：
- 输出结构 `{name, label?, extra?, children?}` 直接兼容 D3.js 的 `d3.hierarchy()`
- `name` 字段存储 AST 节点类型名，用于 D3 节点的颜色编码
- `label` 字段存储节点的语义值（函数名 `"main"`、运算符 `"+"` 等）
- `extra` 字段存储辅助信息（`"mut=True"`、`"stmts=3"`），渲染在节点底部
- 子节点递归处理，通过 `isinstance` 按节点类型提取

**流水线端点 `/api/pipeline`** — 一次性串联所有阶段：

```python
@app.route("/api/pipeline", methods=["POST"])
def api_pipeline():
    data = request.get_json()
    source = data.get("source", "")
    result = {"source": source}

    # 1. Lexer
    lexer = Lexer(source)
    tokens = lexer.tokenize()
    result["tokens"] = [_token_to_dict(t) for t in tokens]

    # 2. Parser（携带追踪器以支持逐步动画）
    tracer = ParserTracer()
    parser = Parser(source, tracer=tracer)
    ast, parse_errors = parser.parse()
    result["trace"] = tracer.get_log()
    result["trace_events"] = [_trace_event_to_dict(e) for e in tracer.events]
    result["ast"] = _ast_to_dict(ast)

    # 3. Semantic Analysis
    analyzer = SemanticAnalyzer()
    sem_errors = analyzer.analyze(ast)
    result["symbol_table"] = _extract_symbol_table(analyzer)

    # 4. IR Generation
    ir_gen = IRGenerator()
    quads = ir_gen.generate(ast)
    result["quads"] = [_quad_to_dict(q) for q in quads]

    # 5. Assembly Generation
    asm_gen = AssemblyGenerator(quads)
    result["assembly"] = asm_gen.generate()

    # 6. Statistics
    result["stats"] = _compute_stats(ast, tokens, quads)
    return jsonify(result)
```

#### 4.2.2 纯文本 AST 可视化 — `visualization.py`

**定位**：辅助后端计算统计信息（`_compute_stats` 中的节点计数），同时可用于调试输出。

**核心类 `ASTVisualizer`**：

```python
class ASTVisualizer:
    def visualize(self):
        """Generate a visual representation of the AST (tree format)"""
        if self.ast is None:
            return "No AST to visualize"
        lines = []
        self._visualize_node(self.ast, "", True, lines)
        return "\n".join(lines)

    def _visualize_node(self, node, prefix, is_last, lines):
        """Recursively visualize a node and its children"""
        if node is None:
            return
        if isinstance(node, ASTNode):
            node_str = self._get_node_repr(node)
            connector = "└── " if is_last else "├── "
            lines.append(f"{prefix}{connector}{node_str}")
            children = self._get_children(node)
            child_prefix = prefix + ("    " if is_last else "│   ")
            for i, child in enumerate(children):
                self._visualize_node(child, child_prefix, i == len(children)-1, lines)
```

使用 Unicode 树形字符（`├──`, `└──`, `│`）生成可读性高的纯文本 AST 树，适用于日志导出和快速调试。

#### 4.2.3 D3.js 树形可视化 — `templates/index.html`

前端是单一 HTML 文件（~1240 行），所有 CSS 和 JavaScript 均内联，无需额外资源加载。

##### (1) AST 树渲染 — `renderAST()`

这是可视化子系统的核心函数，使用 D3.js v7 将 AST 节点树渲染为交互式树形图：

```javascript
function renderAST() {
  if (!S.ast) return;
  const svg = document.getElementById('ast-svg');
  const ctn = document.getElementById('ast-container');
  const w = ctn.clientWidth, h = ctn.clientHeight;
  svg.innerHTML = '';

  // Step 1: 构建 D3 层级数据（从扁平 JSON 转为 D3 可处理的层级结构）
  const root = d3.hierarchy(S.ast, d => d.children);

  // Step 2: 使用 d3.tree 布局，设置节点水平和垂直间距
  const tree = d3.tree().nodeSize([200, 65]);
  tree(root);

  // Step 3: 计算居中偏移，使树在 SVG 中水平居中
  let minX = Infinity, maxX = -Infinity;
  root.each(d => { if(d.x < minX) minX = d.x; if(d.x > maxX) maxX = d.x; });

  // Step 4: 创建可缩放容器（绑定 d3.zoom 事件）
  const g = d3.select(svg).append('g')
    .attr('transform', `translate(${Math.max(100, (w-(maxX-minX))/2 - minX)}, 40)`);
  S.astZoom = d3.zoom().scaleExtent([0.3, 3])
    .on('zoom', e => g.attr('transform', e.transform));
  d3.select(svg).call(S.astZoom);

  // Step 5: 绘制连接边（使用 linkVertical 生成垂直曲线路径）
  g.selectAll('.ast-link').data(root.links()).join('path')
    .attr('d', d3.linkVertical().x(d => d.x).y(d => d.y))
    .attr('fill', 'none').attr('stroke', '#c1c6d7').attr('stroke-width', 1.5);

  // Step 6: 绘制节点组（圆角矩形 + 文本标签）
  const nodes = g.selectAll('.ast-node').data(root.descendants()).join('g')
    .attr('transform', d => `translate(${d.x}, ${d.y})`)
    .on('mouseover', (e, d) => { showASTNode(d); showTip(e, d); })
    .on('mouseout', hideTip);

  // 节点矩形（宽度自适应内容）
  nodes.append('rect')
    .attr('width', d => Math.max(60, (d.data.name.length + (d.data.label||'').length) * 7 + 24))
    .attr('height', d => d.data.label ? 38 : 24)
    .attr('x', d => -计算出的宽度/2)
    .attr('y', d => d.data.label ? -19 : -12)
    .attr('fill', d => nColor(d.data.name).bg)       // 按节点类型着色
    .attr('stroke', d => nColor(d.data.name).border)
    .attr('rx', 5).attr('ry', 5);                     // 圆角 5px

  // 节点类型名
  nodes.append('text').attr('dy', d => d.data.label ? '-4' : '4')
    .attr('text-anchor', 'middle').attr('font-weight', '600')
    .attr('fill', d => nColor(d.data.name).text)
    .text(d => d.data.name);

  // 节点标签值（如函数名 "main"）
  nodes.filter(d => d.data.label).append('text')
    .attr('dy', '14').attr('text-anchor', 'middle')
    .attr('fill', '#555').text(d => d.data.label);

  // 附加信息（如 "mut=True", "stmts=3"）
  nodes.filter(d => d.data.extra).append('text')
    .attr('dy', '26').attr('text-anchor', 'middle')
    .attr('fill', '#999').text(d => d.data.extra);
}
```

**渲染流程**：

```
后端 AST (_ast_to_dict) → JSON → d3.hierarchy() → d3.tree() 布局计算
    → SVG 元素创建 → 边绘制 (linkVertical) → 节点绘制 (rect + text)
    → d3.zoom() 交互绑定
```

**关键设计点**：
- 使用 `d3.hierarchy()` + `d3.tree().nodeSize()` 完成树形布局的自动计算
- 通过 `d3.zoom().scaleExtent([0.3, 3])` 实现缩放和平移
- 节点宽度根据文本长度动态计算（`(name.length + label.length) * 7 + 24`）
- 节点颜色通过 `nColor()` 函数映射：声明节点→蓝色系、语句→绿色系、控制流→紫色系、表达式→琥珀色系

##### (2) 语法树渲染 — `drawPTree()`

语法树视图基于解析器追踪事件（非 AST）构建，展示递归下降解析中的函数调用层级：

```javascript
function buildParseTree(events) {
  /* 从解析器追踪事件构建层级树。
     每个 'enter' 事件创建一个父节点，'consume' 事件作为叶子，
     'exit' 事件关闭当前父节点。 */
  const root = { name: 'program', label: '', children: [] };
  const stack = [root];
  for (const e of events) {
    if (e.type === 'enter') {
      const node = { name: e.func_name, label: e.rule || '', children: [] };
      stack[stack.length - 1].children.push(node);
      stack.push(node);
    } else if (e.type === 'consume') {
      const val = e.message.replace('消费 Token [', '').replace(']', '');
      stack[stack.length - 1].children.push({ name: val, label: e.token_info || '' });
    } else if (e.type === 'exit') {
      if (stack.length > 1) stack.pop();
    }
  }
  return root;
}
```

该函数通过栈模拟递归下降的执行过程：`enter` 对应函数调用（入栈），`exit` 对应函数返回（出栈），`consume` 对应读取一个 Token（叶子节点）。这样可以从平坦的事件序列重建出完整的调用树结构。

##### (3) 解析过程逐步动画 — LR View

**设计思路**：将解析器的递归下降执行过程记录为事件序列，前端逐步回放，展示每一步的调用栈变化和文法规则。

**前端动画控制**：

```javascript
let lrCurrent = 0, lrTimer = null;

function lrNext() {
  if (lrCurrent < lrEvents.length - 1) {
    lrCurrent++;
    renderLRStep(lrCurrent);
  }
}

function lrPlay() {
  if (lrTimer) return;
  lrTimer = setInterval(() => {
    if (lrCurrent < lrEvents.length - 1) { lrNext(); }
    else { lrPause(); }
  }, Math.max(50, 500 - S.lrSpeed * 100));  // 速度可调
}

function lrPause() {
  if (lrTimer) { clearInterval(lrTimer); lrTimer = null; }
}
```

**状态模型**：使用 `lrCurrent` 游标标记当前步骤，`renderLRStep(idx)` 每次读取 `lrEvents[idx]` 更新界面：
- 步骤列表高亮当前步骤
- 调用栈区显示当前栈快照（`e.stack`）
- 文法规则区显示当前规则（`e.rule`）
- 进度条反映整体进度

播放速度可调（`S.lrSpeed` 范围 0-4），映射为 `setInterval` 的间隔时间 50ms–500ms。

#### 4.2.4 语法着色系统

源代码的词法着色通过 CSS 类实现，在前端 `renderLexer()` 函数中将每个 Token 包裹在对应的 `<span>` 中：

```css
/* 语法着色 CSS 类 */
.syn-kw    { color: #0058bc; font-weight: 700; }   /* 关键字 */
.syn-id    { color: #1a1a2e; }                      /* 标识符 */
.syn-num   { color: #c62828; font-weight: 500; }    /* 数值 */
.syn-op    { color: #717786; }                      /* 运算符 */
.syn-delim { color: #8e8e93; }                      /* 界符 */
.syn-type  { color: #0f9d58; font-weight: 600; }    /* 类型 i32 */
```

Token 表也通过 CSS 分类着色，同时支持交互联动——悬停 Token 表中的行时，源代码中对应 Token 高亮，反之亦然：

```javascript
// Token 表行
.tb-kw  { background: #d8e2ff; color: #004493; }   // 关键字标签
.tb-id  { background: #e8e8ed; color: #414755; }   // 标识符标签
.tb-num { background: #fce4d6; color: #a34400; }   // 数值标签
.tb-op  { background: #fef0d6; color: #9a6200; }   // 运算符标签
```

### 4.3 前端页面布局

整个 Web 前端是一个单页应用（SPA），包含 7 个视图面板，通过左侧导航栏切换：

```
┌──────────────────────────────────────────────────────────┐
│  NavBar: [Open] [Save] | [Lexer] [Parser] [Run All]     │  ← 顶部导航栏
├────┬──────────────────────────────────────┬─────────────┤
│    │                                      │             │
│ 导 │     主内容区（6个视图面板切换）          │ 详情面板    │
│ 航 │                                      │ (视图中      │
│ 栏 │   Editor / Lexer / Parser /          │  各有不同)   │
│    │   LR / AST / Issues                  │             │
│    │                                      │             │
└────┴──────────────────────────────────────┴─────────────┘
```

布局采用 Tailwind CSS 的 Flexbox 实现，主要结构：

```html
<body class="flex flex-col h-screen">
  <header class="h-11 ...">...</header>                    <!-- 顶部固定导航 -->
  <div class="flex flex-1 overflow-hidden">
    <nav class="w-sidebar_w ...">...</nav>                 <!-- 左侧导航栏 -->
    <main class="flex-1 flex overflow-hidden">
      <div id="view-editor" class="view-panel active">...</div>  <!-- 各视图面板 -->
      <div id="view-lexer" class="view-panel">...</div>
      ...
    </main>
  </div>
</body>
```

视图切换通过 `active` CSS 类控制：

```css
.view-panel { display: none !important; }
.view-panel.active { display: flex !important; }
```

### 4.4 颜色编码系统

AST 节点采用**按类别分色**的方案，定义在 `nColor()` 函数中：

| 节点类别 | 节点类型 | 背景色 | 边框色 | 文本色 |
|----------|----------|--------|--------|--------|
| 声明    | Program, FunctionDecl, VarDecl | `#e3f2fd` 蓝 | `#90caf9` | `#0d47a1` |
| 语句    | Block, ReturnStmt, ExprStmt | `#e8f5e9` 绿 | `#a5d6a7` | `#1b5e20` |
| 控制流  | IfStmt, WhileStmt, ForStmt | `#f3e5f5` 紫 | `#ce93d8` | `#4a148c` |
| 表达式  | BinaryExpr, LValue, FuncCall | `#fff8e1` 琥珀 | `#ffe082` | `#e65100` |
| 字面量  | NumberLiteral | `#fce4ec` 红 | `#f48fb1` | `#880e4f` |
| 类型    | Type, ArrayType | `#e0f2f1` 青 | `#80cbc4` | `#004d40` |

语法树节点同样按类别分色（`pColor()` 函数），以区分解析器函数调用类型。

---

## 五、设计亮点与总结

1. **单页应用架构**：所有前端资源（HTML/CSS/JS）集中在单一 `index.html` 中，无外部 JavaScript 框架依赖，部署时只需携带一个静态文件。

2. **数据驱动可视化**：前端所有视图基于统一的 JSON 数据源（由 `_ast_to_dict()` 序列化），视图与数据解耦，新增视图类型无需修改后端。

3. **零额外可视化依赖**：树形图使用 D3.js v7（通过 CDN 加载），不依赖 matplotlib、graphviz 等重量级科学计算库。

4. **交互丰富**：AST/语法树支持缩放（0.3x–3x）、平移、悬停 Tooltip；Token 表支持点击/悬停联动高亮；解析过程支持逐步动画（上一/下一/播放/暂停/调速）。

5. **教育导向设计**：解析过程逐步动画展示递归下降解析的完整执行轨迹，调用栈可视化帮助理解函数调用层次，文法规则实时显示连接理论与实现——专为编译原理课程教学而设计。
